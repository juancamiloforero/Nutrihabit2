<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nutrihabit2">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo2"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo2"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".menuPrincipal.ui.detalleComida.DetalleCom"></activity>
        <activity
            android:name=".menuPrincipal.menuPrincipal"
            android:label="@string/title_activity_menu_principal"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".seguimiento.SeguimientoListaActivity" />
        <activity android:name=".consumoDiario.ConsumoRegistroActivity" />
        <activity android:name=".infoBasica.ImcActivity" />
        <activity android:name=".infoBasica.DatosBasicosActivity" />
        <activity android:name=".BienvenidaActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>